# 设计语言 v1

视觉参考来源：MiniMax H3「Director Studio」(项目库页 + 启动引导页两张截图，用户 2026-09-08 直接提供)。

**范围声明**：这份文档只提取 Director Studio 的**视觉系统**——配色、圆角、按钮/卡片/徽标形状、排版节奏、分割线用法。它的页面结构（项目库/剧本库/角色库/AI 设置……）、启动检查清单、视频制作相关的功能和文案**都不是参考对象**，跟本项目（Agent 套壳 + 本地记忆）没有关系，不要照搬。

---

## 一、设计原则

1. **红是决断色，不是背景色。** 参考截图里大面积仍然是白/浅灰/黑，红色只出现在：主按钮、当前激活态、进度条、Logo、极少数状态点。不要把红做成大面积色块。
2. **直角为主，小圆角克制使用，药丸形状专门留给状态和标签。** 按钮和卡片用小半径的矩形（有转角但很收敛），只有筛选 Tab、状态徽标、IP/端口这类"标签型"元素才用完全圆角的药丸形状。二者不能混用——按钮变药丸、标签变直角都会破坏这套语言的识别度。
3. **细线分割，弱阴影。** 卡片靠 1px 极浅灰边框和分割线组织信息，不靠重阴影堆叠层次。整体观感偏"打印品/仪表盘"，不是"悬浮卡片"。
4. **等宽字体是技术感的调味料。** 标题、正文用人文无衬线；但凡是端口号、百分比、IP、耗时、日志这类精确数值，切到等宽字体。这一条恰好和 boujoy-harness 现有的 `--mono` 用法（token 计数、耗时等）是一致的，不用改字体栈，只需要更严格地执行"数值上 mono"这条规则。
5. **强调色只有一个红，不叠加第二个高饱和色。** 参考截图里绿色仅用于"就绪/成功"这一个语义，克制到近乎单色系。

---

## 二、色彩

以下色值是从两张截图目测估计得出（不是像素级取色），落地时按这个色相/明度去微调即可，不必当作精确规范。

### 浅色模式（截图直接展示的版本）

| Token | 估计值 | 用途 |
|---|---|---|
| `--bg` | `#F7F6F2` | 页面背景，偏暖的浅米白，不是纯白 |
| `--surface` | `#FFFFFF` | 卡片、面板背景 |
| `--surface-active` | `color-mix(in srgb, var(--red) 8%, white)` | 侧边栏当前项、hover 态背景 |
| `--text` | `#161616` | 标题、正文主文字 |
| `--text-muted` | `#6E6E6E` | 次要说明文字 |
| `--text-faint` | `#9A9A9A` | 时间戳、辅助数值 |
| `--line` | `#E8E5E0` | 卡片边框、分割线，暖灰、极低对比 |
| `--red` | `#E1362B` | 唯一强调色：主按钮、进度条、激活态、Logo |
| `--red-strong` | `#C82A20` | 主按钮 hover/active |
| `--success` | `#1E9E5C` | 就绪/成功状态点、勾选图标 |

### 深色模式（用户确认为"红黑"，未在截图中展示，按同一色相外推）

| Token | 估计值 | 用途 |
|---|---|---|
| `--bg` | `#0A0A0A` | 页面背景，接近纯黑（对应 MiniMax 官方设计系统公开的品牌黑 `#0a0a0a`，作为外推依据） |
| `--surface` | `#161616` | 卡片、面板背景，比背景略提亮一档 |
| `--surface-active` | `color-mix(in srgb, var(--red) 14%, black)` | 侧边栏当前项、hover 态背景 |
| `--text` | `#F2F2F2` | 标题、正文主文字 |
| `--text-muted` | `#9A9A9A` | 次要说明文字 |
| `--text-faint` | `#6E6E6E` | 时间戳、辅助数值 |
| `--line` | `#2A2A2A` | 卡片边框、分割线 |
| `--red` | `#FF4433` | 同一红色在深色背景下略提亮以保证对比度 |
| `--red-strong` | `#E1362B` | 主按钮 hover/active（即浅色模式的主红） |
| `--success` | `#33C67D` | 就绪/成功，同样在深色下略提亮 |

红色本身不必分浅深两套色相——只是深色背景下同一个红需要提高一点亮度才不会显灰，这是常规的深色模式对比度处理，不是两个不同的品牌色。

---

## 三、圆角

Director Studio 的圆角其实和 boujoy-harness 现有的 `--r-sm/md/lg`（6 / 10 / 14px）量级接近，不需要大改半径数值本身——真正的变化是**哪种元素允许用圆角、哪种必须用直角**这条规则被执行得更严格：

| Token | 值 | 用在哪 |
|---|---|---|
| `--r-xs` | 6px | 输入框、小型控件 |
| `--r-sm` | 8px | 按钮（主/次按钮统一） |
| `--r-md` | 10–12px | 卡片、面板、弹窗内的分区块 |
| `--r-lg` | 14–16px | 顶层容器、弹窗 |
| `--r-pill` | 999px | **仅限**筛选 Tab、状态徽标（如"待开始"）、IP/端口这类小标签 |

判定口诀：**这个元素是"可以点击去做一件事"（按钮/卡片入口）还是"用来标注一个状态/分类"（Tab/徽标/标签）？** 前者用 `--r-sm`/`--r-md`，后者用 `--r-pill`。参考截图里"新建""进入导演台"是前者（小圆角矩形），"全部""制作中""待开始"是后者（完全药丸）——这个区分本身就是这套语言最容易被抄错的地方，务必保留。

---

## 四、排版

沿用 boujoy-harness 现有的字体栈组合（不需要换字体，只是更明确地按用途分工）：

| 用途 | 字体 | 字重/字号参考 | 示例 |
|---|---|---|---|
| 大标题 | `--display`（SF Pro Display / PingFang SC） | 650–700，clamp(24px,3vw,36px) | "管理和组织你的视频创作项目" |
| 小节标题 | `--display` | 650，14–18px | 卡片内小标题 |
| 正文/说明 | `--read`（系统 UI 字体） | 500，12–13px，`--text-muted` | 卡片描述文字 |
| 小型强调标签 | `--read` 或 `--display` | 600，10px，字距 `.14–.16em`，大写/红色 | "创作环境启动中"、boujoy 现有的 `.section-kicker` |
| 技术数值 | `--mono` | 500，10–12px | IP、端口、百分比、耗时、日志行 |

关键行为：**任何"有精确值"的东西都切等宽**——不只是端口号，token 用量、百分比、倒计时、时间戳都算。这条在 boujoy-harness 现有 CSS 里已经部分做到（Monitor 页 token 数字用 mono），新增页面要保持一致，不要因为图省事用正文字体写数字。

---

## 五、组件形状规则

- **主按钮**：`--red` 实心填充，白字，`--r-sm` 圆角，无边框。hover 用 `--red-strong`。
- **次按钮/描边按钮**：`--surface` 背景，`--line` 描边，`--text` 文字，`--r-sm` 圆角。hover 边框颜色略微向 `--red` 靠近，而不是整体变红底——只有主按钮才允许红色实心。
- **筛选 Tab / 状态徽标**：`--r-pill`，未激活态浅底浅字（`--surface` / `--text-muted`），激活态 `--red` 实心白字。
- **卡片**：`--surface` 背景 + 1px `--line` 描边 + `--r-md`，阴影收敛为极轻的一层（不用现有 `--shadow-lg` 那种双层深阴影，参考截图的卡片几乎靠边框在分层，阴影只是提示可点击，不是主要层次手段）。
- **进度条**：轨道用 `--line` 浅底，填充用 `--red`，两端全圆角（`height/2`），整体偏粗（6–8px 高），不是纤细的 2px 线。
- **状态点**：4–6px 实心圆点，`--red` = 离线/等待，`--success` = 就绪，无边框。
- **分割线**：1px `--line`，用在卡片内部分区、列表行之间，比阴影更常用来表达"这里分区了"。
- **Logo/图标容器**：`--r-md` 圆角方块，`--red` 实心底，内部图形用白色线条，不用照搬 Director Studio 的具体图案，只保留"红色圆角方块 + 白色简单图形"这个形状逻辑。

---

## 六、落地到 boujoy-harness 的 token 映射

boujoy-harness 现有 `web/app.css` 的 `:root` 是一套暖色系"编辑感/拼贴感"配色（`--ink #2a2620`、`--paper #faf7f1`、`--blue #4a6b9f`、`--pink #b56a86`、`--acid #5e8c61`、`--cyan #4f8f8a`）。按这份设计语言迁移时：

| 现有 token | 现有值（浅色） | 建议新值 | 说明 |
|---|---|---|---|
| `--paper` | `#faf7f1` | `#F7F6F2` | 背景微调得更中性，少一点"信纸"感 |
| `--paper-2` | `#fffdf9` | `#FFFFFF` | 卡片背景改纯白 |
| `--ink` | `#2a2620` | `#161616` | 主文字改近黑，不带棕色调 |
| `--blue`（当前唯一的"主操作色"） | `#4a6b9f` | `#E1362B`（即新的 `--red`） | **这是核心改动**：主按钮、激活态、焦点环全部从蓝改红 |
| `--pink` / `--acid` / `--cyan`（当前分散用于不同页面强调） | 三个不同色相 | 收敛为只用 `--red` + `--success`（`#1E9E5C`） | 减少强调色数量，符合原则五 |
| `--line` | `rgba(58,50,38,.14)` | `#E8E5E0` | 改暖灰实色，边框存在感更明确 |
| `--r-sm/md/lg` | `6 / 10 / 14px` | `8 / 10–12 / 14–16px` | 数值上小调，重点是执行第三节"按钮 vs 标签"的圆角分配规则 |
| `--shadow` / `--shadow-lg` | 双层深阴影 | 单层更轻的阴影 | 卡片层次更多靠 `--line` 边框，阴影只是轻微提示 |
| 深色模式 `--paper` | `#141519` | `#0A0A0A` | 深色背景更接近纯黑，对应"红黑"里的"黑" |

字体栈（`--display` / `--read` / `--mono`）、`.section-kicker` 这类小标签样式、`nav-cut`/`mini-cut` 的按钮尺寸系统都不需要重做，只是配色和圆角分配规则要按上表调整。

这份文档只定义视觉系统本身，具体哪个页面、哪个组件先改，见 [ROADMAP.md](ROADMAP.md) 里的视觉迁移阶段。
