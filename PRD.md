# Boujoy Harness + Bok — PRD

版本：v1.0（2026-09-08，Bok 记忆工作台并入之后）

## 0. 背景

这是同一个人（本仓库作者）独立做过三次的同一个方向：XU4N Harness、Boujoy Harness、Bok。三者概念高度重合——都是"给本地 agent CLI 套一个可用的桌面壳 + 给它一份能持续积累的本地记忆"。2026-09-07 决定不再续写 XU4N Harness 那份代码，改为以 **Boujoy Harness 为基座**、**把 Bok 的记忆引擎并进来**，在这个合并后的基础上继续往下做。本 PRD 描述的就是合并之后这个产品，不是三个项目里的任何一个原版。

## 1. 产品定位

**一句话**：本地运行、围绕你已经在用的 agent CLI（dsh / DeepSeek Harness）搭的桌面工作台，自带一份不依赖任何云端、可审阅可撤销的长期记忆。

Boujoy Harness 提供"能长期用下去的界面"：稳定的多轮对话 UI、审批队列、人设/风格预设、用量监控、便携分发。Bok 提供"记得住的大脑"：把日常使用中的对话、文件、速记压缩成可复用的知识卡片和一份关于用户本人的持续画像，下次对话不用重新解释背景。合并前，前者有壳没记忆，后者有记忆没有像样的对话壳——现在两者在同一个进程里跑。

## 2. 目标用户

只有一个用户画像，就是本仓库作者本人：日常用 dsh 这类 agent CLI 做真实工作，想要一个不进 Electron/不依赖云端账号、数据完全在自己机器上、UI 顺手到愿意天天开着的壳，并且愿意持续往里加自己的想法。不考虑多用户、团队协作、移动端。

## 3. 核心架构

```
┌─────────────────────────────────────────────┐
│ 原生壳（macOS: Swift+WKWebView / Windows: PowerShell）│
│   负责：拉起下面两个服务、开窗口、自愈重启、便携打包        │
└───────────────────┬───────────────────────────┘
                     │
┌────────────────────▼───────────────────────────┐
│ boujoy_server.py（本地网关，:8766，纯 stdlib）        │
│   反代/桥接三个下游：                                │
│   ① dsh knowledge 实例 :3080（挂 vault，正常模式）     │
│   ② dsh clean 实例 :3081（不挂 vault，纯净模式）       │
│   ③ BokUIBridge → bok_core 内部 HTTP API（:随机端口） │
└──────────┬───────────────────┬──────────────────┘
           │                   │
   ┌───────▼──────┐    ┌───────▼──────────────┐
   │ dsh / DeepSeek │    │ bok_core             │
   │ Harness        │    │ vault（Markdown）+    │
   │（外部，已安装）  │    │ Personal Core（vault 外） │
   └────────────────┘    └───────────────────────┘
```

前端是一个纯 vanilla JS 的单页应用（`web/app.js` + `web/index.html`），七个页面按左侧导航切换，页面内容和状态都在同一个 DOM/JS 上下文里，没有前端框架、没有构建步骤。

## 4. 现状（v1.0 已具备的能力）

### 4.1 Agent 页（01，dsh 对话壳）
流式对话、图片附件、权限模式/模型/推理强度切换、"打断态"消息可选立即发送或排队、会话 fork/重命名/归档/软删除、计划/队列/子代理实时侧栏、Goal 任务模式。会话状态走事件溯源（序号去重、断点续传），审批/提问走队列不会互相打断或抢焦点。

### 4.2 知识库页（02，vault 浏览）
Markdown vault 的 CJK 感知搜索、反链阅读、关系图谱、健康中心、本地视频播放，AI 判重的知识卡片捕获。这是 Boujoy 自己原生的、比较轻量的 vault 浏览器，和下面的 Bok 页面并存，不冲突。

### 4.3 专家 / 风格页（03 / 04）
人设和语气预设，存成 vault 里的 Markdown + YAML，可以直接派发进新的一轮对话。

### 4.4 监控页（05）
Token/缓存用量、上下文占用、运行轨迹、推理强度切换。

### 4.5 新闻页（06）
自建 RSS 聚合（量子位/MIT Tech Review/HN/OpenAI/Google AI/TechCrunch 等），同源缩略图代理防止被滥用成开放代理。

### 4.6 Bok 页（07，记忆工作台 —— 本次新增）
- **工作台**：语义+关键词混合搜索（`bok_core.search`）、随手记捕获、记忆收件箱（后台产生的候选记忆，逐条采用/忽略）
- **关于我**：Personal Core 画像看板，按类型（身份/工作偏好/决策模式/项目经历……）展示已形成的理解，需要人工确认的高风险/冲突/低置信条目单独列出，逐条确认/拒绝
- 后端：`bok_core`（vendor 进 `web/bok_core/`，纯 stdlib、零第三方依赖）通过 `BokUIBridge` 挂进 `boujoy_server.py`，复用现有 vault 和访问码校验；Personal Core 存储在 vault 和任何 git 仓库之外的独立目录（`~/.bok-personal-core`）
- 已验证：真实读写 vault（速记落盘为 Markdown 文件，Boujoy 自己的知识库页面也能看到）、搜索命中真实内容、无本地模型时记忆判定优雅降级为排队等待而不是报错丢内容

### 4.7 分发
无安装包，"便携包"模式：`.app`/exe + `runtime/`（含便携 Python 和 dsh 运行时）+ `vault/` + 一键启动脚本。macOS 处理了 Gatekeeper App Translocation；Windows 支持标注为 Beta。

## 5. 设计语言

正在从 Boujoy 原有的暖色"编辑感"配色迁移到红黑主色调、克制圆角的新视觉系统，细节见 [DESIGN-LANGUAGE.md](DESIGN-LANGUAGE.md)。这份 PRD 里的功能范围不受迁移影响，视觉迁移作为独立的路线图阶段推进，见 [ROADMAP.md](ROADMAP.md)。

## 6. 范围内但还没做的

按优先级排序，详细任务拆解见 [ROADMAP.md](ROADMAP.md)：

1. 把新的红黑设计语言实际应用到现有六个页面（目前只有 Bok 页按新语言写的，其余页面还是老配色）
2. 把 `bok_core` 注册成 dsh 的 MCP 记忆源，让 Agent 对话自动读写记忆，而不是只能在 Bok 页面手动操作
3. 作者自己后续要加的功能（暂未定义，见 ROADMAP 里的占位阶段）

## 7. 明确不做的（非目标）

- **不做团队/多用户协作**，Personal Core 和 vault 都是单机单用户设计
- **不做移动端原生 app**，phone 访问维持现状的"局域网 PWA 配对"模式
- **不把原生壳迁移到 Tauri/Electron**，继续用 Swift（macOS）/ PowerShell（Windows）的轻量套壳路线
- **不合并 Boujoy 原生知识库页和 Bok 工作台**，两套 vault 浏览体验先并存，等 Bok 页面用顺了再评估要不要收敛成一个
- **不引入云端账号体系或遥测**，延续 Boujoy 和 Bok 双方原有的"零遥测、本地优先"立场
- **不使用 Bok-Desktop 的 Tauri 打包方案或 AI-Second-Brain-UI 的独立启动方式**，这两个仓库只作为代码参考来源，不作为运行时依赖

## 8. 成功标准

没有对外指标（单用户产品），衡量标准是主观但具体的：
- 日常真实工作时会打开这个壳而不是裸用 dsh CLI
- 隔几天回来打开 Bok 页面，记忆收件箱里的候选是"确实想留下"的，不是需要批量清空的噪音
- 出现过的一个决策/偏好，不需要在新会话里再解释一次

## 9. 开放问题

- Personal Core 需要本地模型（Ollama）或配置 API key 才能真正把候选记忆判定成结论；作者本机目前两者都未配置，记忆收件箱会一直停留在"排队等待"状态——需要决定接哪一个
- Bok 原有的 MCP 十个工具（`bok_search`/`bok_capture_memory`/… 等）尚未接入 dsh，接入后 dsh 侧的权限/审批模型要不要覆盖到 Bok 的写操作，还没设计
- 视觉迁移是否需要保留一个"经典配色"开关，还是直接全量切换，未决定
